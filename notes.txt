REPL's are cool

have access to a webservice REPL while working on a UI is awesome

SSL

USEFUL FUNCTIONS;
clojure.core.select-keys
clojure.core.ex-info, clojure.core.ex-data

USE refer-clojure exclude

CREATE DATABASE `entity` CHARACTER SET utf8 COLLATE utf8_general_ci;

;; BUILDING CLJS

lein clean; lein with-profile type figwheel

lein clean; lein with-profile production figwheel
